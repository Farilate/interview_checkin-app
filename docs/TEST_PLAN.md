# 测试计划

## 1. 当前状态与方法

第 2–7 节为整体验收清单，第 9 节汇总已有验证结果，第 10 节列出当前认证模块仍需执行的真实存储验收。自动化回归通过不代表所有阶段已完成。

测试原则：

- 连续天数算法使用可注入 `Clock` 做确定性测试。
- 数据库唯一约束、事务和 Redis 使用真实 MySQL/Redis 验证。
- UniApp H5 使用真实 REST API 做端到端验收。
- 不用 H2、内存 Map 或前端 Mock 代替真实 MySQL/Redis 的核心验收。
- 测试使用独立数据库、Redis 库和测试账户，不污染正式演示数据。

本计划分为“必做测试”和“可选高级测试”。项目先通过必做测试，再视时间补充高级验证。

## 2. 必做测试：登录、权限与基础功能

登录、当前用户和登出的自动化覆盖见第 9 节，真实存储验收见第 10 节。使用存储替身的回归测试不能替代真实 MySQL/Redis 联调。

| 编号 | 场景 | 预期 |
| --- | --- | --- |
| M01 | 正常登录 | HTTP 200/code=0；Redis 建立 Session；TTL 接近配置值（默认 7200 秒）；Token 可访问受保护 API |
| M02 | 错误密码 / 不存在用户名 | 均返回 401/40102；外部提示一致；不创建有效 Session |
| M03 | Token 缺失、伪造或过期 | 401/40101；不能访问或修改业务数据 |
| M04 | 创建打卡项并重新查询列表 | HTTP 201；真实写入 MySQL；列表可查；刷新后仍存在 |
| M05 | 参数校验 | 空白/超长名称、非法分页等返回 400；无非法写入 |
| M06 | 用户隔离 | 用户 A 无法查询或打卡用户 B 的习惯；他人资源与不存在资源均返回 404 |
| M07 | 空列表与分页 | 无数据时 `items=[]`；分页参数合法；排序稳定 |
| M32 | 当前用户与登出 | 返回安全用户资料及字符串 ID；登出撤销当前会话，重复登出返回 40101，其他会话不受影响 |
| M31 | 习惯名称唯一 | 同一用户重复名称创建返回 HTTP 409/code=40901；不同用户可创建同名习惯；数据库 uk_habits_user_name 兜底 |

## 3. 必做测试：每日打卡与并发安全

| 编号 | 场景 | 预期 |
| --- | --- | --- |
| M08 | 第一次 `PUT /checkins/today` | HTTP 200/code=0，`created=true`；MySQL 新增 1 条记录 |
| M09 | 同日重复调用 PUT | HTTP 200/code=0，`created=false`；返回同一已有记录；MySQL 总计仍 1 条 |
| M10 | 快速重复点击/网络重试 | 不产生重复记录；前端按钮禁用不是最终正确性条件 |
| M11 | 并发打卡 | 对同一用户、习惯、业务日期并发发起多次请求；最终 MySQL `COUNT(*)=1`，仅一次创建成功，其余返回已有记录 |
| M12 | 直接验证数据库唯一约束 | 重复插入相同 `(user_id, habit_id, checkin_date)` 必须命中 `uk_checkin_user_habit_date` |
| M13 | 非唯一约束数据库异常 | 外键、连接、事务等错误不得当成“重复打卡成功” |

## 4. 必做测试：连续天数与日期

下列 D 为后端业务日期。

| 编号 | D / 历史记录 | 预期 |
| --- | --- | --- |
| M14 | D=2026-09-17；09-15、09-16、09-17 | `checkedIn=true`，连续 3，结束日 09-17 |
| M15 | D=2026-09-17；09-14、09-15、09-17 | 中间断签，连续 1 |
| M16 | D=2026-09-17；09-15、09-16 | 今天未打卡，连续 2，结束日 09-16 |
| M17 | D=2026-09-17；只有 09-15 或无记录 | 连续 0，结束日 null |
| M18 | D=2026-03-01；02-28、03-01 | 跨月连续 2 |
| M19 | D=2027-01-01；2026-12-31、2027-01-01 | 跨年连续 2 |
| M20 | 浏览器或操作系统时区变化 | 同一服务端时间点的业务日期仍由 `Asia/Shanghai` 决定 |

## 5. 必做测试：Redis 与缓存

| 编号 | 场景 | 预期 |
| --- | --- | --- |
| M21 | 今日状态/连续天数第一次查询 | Redis Miss → 查询 MySQL → 写入短 TTL 缓存 |
| M22 | 相同查询再次执行 | 能命中业务缓存，结果正确；TTL 不因普通读取滑动续期 |
| M23 | 删除 today/streak Key 后再次查询 | 能从 MySQL 恢复正确数据并重建缓存；真实打卡记录不丢失 |
| M24 | 已缓存旧状态后成功打卡 | MySQL 提交后删除 today/streak 缓存；写接口直接返回新真实结果；后续查询可得到新值 |
| M25 | MySQL 事务回滚 | 不新增打卡记录，也不能写入“已打卡”业务缓存 |

## 6. 必做测试：API、安全与端到端

| 编号 | 场景 | 预期 |
| --- | --- | --- |
| M26 | 统一响应 | 成功及 400/401/404/405/500/503 均符合 `code/message/data` 结构 |
| M27 | 密码与敏感信息 | MySQL 只存 BCrypt 哈希；源码、SQL、日志不包含真实密码或完整 Token |
| M28 | H5 完整链路 | 登录 → 创建 → 列表 → PUT 打卡 → 今日状态/连续天数 → 刷新，全程真实 REST API |
| M29 | 前端异常处理 | 请求失败/超时不显示虚假成功；401 清理 Token；503 不错误地当成退出登录 |
| M30 | 构建与启动 | 后端 Maven 测试/构建成功；前端 H5 构建成功；按 README 能完成数据库初始化和启动 |

## 7. 可选高级测试

这些测试用于加分和增强工程性，不作为第一版完成的前置条件：

| 编号 | 场景 | 目标 |
| --- | --- | --- |
| A01 | 20+ 并发请求重复多轮 | 更强地验证唯一约束和事务冲突处理 |
| A02 | 失败事务后重新读取已有记录 | 验证不会在 rollback-only 事务中错误读取并返回假成功 |
| A03 | Redis 整体不可用 | 新登录/鉴权不能绕过 Session；已有 MySQL 数据保持完整 |
| A04 | 已鉴权后业务缓存不可用 | 回退 MySQL，业务查询和已提交打卡结果仍正确 |
| A05 | 缓存删除失败 | 记录日志；MySQL 成功不回滚；短 TTL 后缓存最终恢复 |
| A06 | 缓存内容损坏 | 按 Miss 处理并重新从 MySQL 构造正确值 |
| A07 | 闰年 | 2028-02-28、02-29、03-01 连续 3 |
| A08 | 极长连续记录 | 例如 400 天 streak，不被固定最近 30/365 天查询错误截断 |
| A09 | 午夜边界 | 午夜前捕获的请求使用旧业务日期；午夜后的新请求使用新日期 |
| A10 | 超大 BIGINT ID | 前端按字符串处理，不发生 JavaScript 安全整数精度问题 |
| A11 | 数据库临时故障/死锁 | 返回真实错误，不吞掉为重复打卡成功 |
| A12 | CORS 与多 Session | 仅允许配置来源；不同 Session 独立固定到期 |

## 8. 交付记录

后续实际执行时，为每个测试记录：

- 通过 / 失败 / 未执行。
- 测试环境与版本。
- 操作或命令。
- 关键断言。
- 必要的 MySQL / Redis / Network 证据位置。

未执行的测试不得标记为通过。第一版交付以全部“必做测试”通过为目标；“可选高级测试”根据剩余时间补充。

## 9. 已执行验证与覆盖边界

验证记录日期：2026-09-18。下表汇总现有测试，不保留已废止名称规则、旧测试数量及修复前问题清单；历史实现可通过版本记录追溯。

| 测试集 | 数量 | 验证范围 | 最近记录 |
| --- | --- | --- | --- |
| ApiContractTest | 56 | 随机端口真实 HTTP；统一响应、参数校验、JSON 严格解析、错误码、404/405/406/415、no-store 和安全错误消息 | Java 21 下 verify 通过 |
| AuthRepairTest | 9 | 凭证边界、真实 BCrypt、大整数字符串 ID、请求身份及已删除用户会话清理 | Java 21 下 verify 通过 |
| SessionServiceTest | 12 | 摘要键、固定 TTL 调用、不续期、无效身份值、非法 TTL 和 Redis 故障分类 | Java 21 下 verify 通过 |
| AuthControllerTest | 4 | 生产认证组件的 MVC 登录、当前用户、登出、重复登出、多会话隔离及 50301 响应 | Java 21 下 verify 通过 |
| PersistenceIT | 10 | 真实 MySQL 中三个 Mapper、字段映射、分页、用户隔离、用户名/习惯名唯一、打卡唯一及复合外键 | 认证修复前的 mysql-it 回归通过；认证修复后未重跑 |

最近一次公共异常与认证代码验证使用 Java 21.0.12.1 / Maven 3.9.11 / Spring Boot 4.1.1，执行 `verify`：前四组共 81 项，0 失败、0 错误、0 跳过，可执行 JAR 打包成功。报告位于 `backend/target/surefire-reports/`。认证测试使用外部存储替身，不连接真实 MySQL/Redis；生产业务仍使用真实 Mapper 和 Redis 客户端。

PersistenceIT 的最近记录使用隔离 MySQL 26.7.0 实例及专用空库，执行 `-Pmysql-it clean verify`。10 项持久层测试通过，结束后独立查询三表行数均为 0，测试事务已回滚。当前规则为不同用户允许同名、同一用户名称唯一；现有测例不包含多线程并发验证。报告目录为 `backend/target/failsafe-reports/`，构建产物可能被后续 clean 清理。

认证回归执行时工具环境中的 Wrapper 启动失败，使用其缓存的同版本 Maven 并显式指定本机仓库完成验证；这不代表项目 Wrapper 已损坏。Mockito 有动态加载测试代理提示，未影响本次 Java 21 构建结果。

本轮异常映射重构新增 20 项检查：通用框架异常的 7 种状态、容器分派的 7 种状态、3 类服务端校验或配置错误、普通绑定异常、安全日志以及附带未知异常的容器 404。明确业务码、真实路由 404、405 的 Allow、406/415、no-store 和认证测试均继续通过。未知通用状态回退 50001，不推断凭证错误、资源不存在或 Redis/MySQL 故障。

日志校验确认保留异常类型和堆栈位置，但不包含测试注入的敏感消息；响应始终使用安全提示。本轮未修改 SQL、Mapper、Session 主流程或 DTO，没有运行真实 MySQL/Redis 联调。

## 10. 当前认证模块的真实存储待验收项

以下均未完成真实 MySQL/Redis 联调。应使用隔离数据库、独立 Redis 测试环境及测试账户；演示初始化凭证留空，避免完整应用启动时自动插入账户。测试报告不得记录明文密码或完整 Token。

| 场景 | 验收要求 |
| --- | --- |
| 登录及当前用户 | 使用真实 BCrypt 用户登录；完整响应包含 token、tokenType、expiresIn、user；ID 为字符串，资料不含密码哈希 |
| 输入与错误凭证 | 用户名规范化、ASCII 长度和密码 UTF-8 字节限制生效；非法参数返回 40001；合法格式的错误凭证返回 40102，不产生会话 |
| 实际存储与过期 | 检查摘要键、用户 ID 字符串及真实 TTL；重复查询不续期；自然到期后返回 40101，不能从 MySQL 恢复原会话 |
| 登出与多会话 | 当前键被删除，原令牌再次访问或登出返回 40101；同一用户的其他会话仍有效 |
| 并发登出 | 已通过认证的请求可正常完成删除；后到请求按会话状态返回 40101，不误报成功撤销其他会话 |
| 用户已删除 | 受保护请求拒绝身份并撤销当前会话；清理 Redis 失败返回 50301，不放行 |
| 损坏会话 | 非整数、非正数及超出 Long 范围的身份值被清理后返回 40101 |
| Redis 与 MySQL 故障 | Redis 读、写、删除故障返回 50301；数据库连接故障返回 50302，不误删正常会话 |
| 演示账户初始化 | 缺少凭证跳过；合法新账户写入 BCrypt 哈希；已有账户不重设密码；验证并发启动的唯一约束冲突行为 |
| 有效期配置 | 非正数或不能安全转换为毫秒的 TTL 在启动时拒绝 |

CORS、H5、习惯和打卡接口、业务缓存、日期算法及端到端并发属于后续阶段，仍按第 2–7 节和实施计划验收，不作为当前已完成能力。