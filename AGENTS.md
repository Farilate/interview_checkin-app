# AGENTS.md

## 1. 项目说明

本项目是一个全栈「习惯打卡」应用。

技术栈：

- 前端：UniApp + Vue 3
- 演示目标：H5
- 后端：Java + Spring Boot
- 数据库：MySQL
- 缓存 / 登录状态：Redis
- 后端依赖管理：Maven
- 前后端通信：REST API

项目采用前后端分离架构。

开发时优先完成题目要求，不要擅自增加与项目无关的复杂功能或架构。

---

## 2. 必须实现的功能

项目至少支持：

1. 用户登录
2. 创建打卡项
3. 查看当前用户的打卡项
4. 每日打卡
5. 查看当天是否已经打卡
6. 查看当前连续打卡天数

已经实现的业务功能必须使用真实后端接口。

不得使用 Mock 数据伪造已经完成的核心业务。

---

## 3. 前后端联调规则

核心业务流程必须真实经过：

前端页面
-> REST API
-> Spring Boot
-> MySQL / Redis
-> 后端响应
-> 前端更新页面

前端不能只修改页面状态来模拟业务成功。

例如：

用户点击打卡后，必须真正调用后端接口并写入 MySQL。

---

## 4. MySQL 规则

MySQL 是核心业务数据的持久化来源。

至少需要包含：

- 用户数据
- 打卡项数据
- 打卡记录数据

核心业务数据不能只保存在 Redis 中。

同一个用户针对同一个打卡项，在同一天最多只能存在一条打卡记录。

数据库层必须提供能够阻止重复打卡记录的约束。

不能只依赖应用层判断。

数据库结构需要提供 SQL 建表脚本或 migration 脚本。

---

## 5. Redis 规则

Redis 必须真实参与业务。

至少用于：

- 登录状态
- 当日打卡状态缓存
- 连续打卡天数缓存

MySQL 仍然是最终业务数据来源。

Redis Key 必须能够说明：

- Key 的用途
- Key 的命名
- 是否需要 TTL
- TTL 如何设置
- 什么时候写入
- 什么时候更新或删除
- 缓存失效后如何从 MySQL 恢复

数据库写入成功后，需要正确处理相关缓存。

不要出现 MySQL 已经更新，但 Redis 长时间保留旧状态的情况。

---

## 6. 登录与用户身份

用户登录成功后，后端必须能够识别当前登录用户。

需要登录的接口应以后端识别到的当前用户为准。

不要通过前端随意传入 userId 来代替真正的登录身份。

用户只能访问和操作自己的打卡项和打卡记录。

---

## 7. 每日打卡规则

用户可以针对自己的打卡项执行每日打卡。

同一个：

- 用户
- 打卡项
- 日期

最多只能成功打卡一次。

以下情况都不能产生重复记录：

- 快速重复点击
- 浏览器重复提交
- 网络重试
- 并发请求

不能只依赖：

- 前端按钮禁用
- 先查询再插入

来保证并发安全。

最终重复数据保护必须由数据库约束兜底。

后端需要正确处理重复请求和数据库唯一约束冲突。

---

## 8. 连续打卡规则

连续打卡天数必须根据实际日期计算。

不能直接使用：

- 总打卡次数
- 打卡记录总数

作为连续天数。

计算逻辑必须正确处理：

- 连续多天打卡
- 中间漏打一天
- 跨天
- 跨月
- 跨年
- 时区边界

连续天数计算由后端负责。

前端只展示后端返回的结果，不自行实现连续天数算法。

---

## 9. 前端开发规则

前端使用：

- UniApp
- Vue 3
- H5

页面负责：

- 展示数据
- 获取用户输入
- 调用后端接口
- 根据接口结果更新页面

核心业务规则放在后端。

API 请求应集中管理。

不要在大量页面组件中重复编写：

- 后端地址
- 请求逻辑
- 登录状态处理
- 公共 Header
- 错误处理

真实后端接口存在后，不再使用 Mock 数据。

---

## 10. 后端开发规则

Spring Boot 后端负责：

- 用户登录
- 用户身份识别
- 打卡项管理
- 每日打卡
- 当日打卡状态判断
- 连续打卡天数计算
- MySQL 数据访问
- Redis 数据访问
- 并发安全
- REST API

保持清晰的代码分层。

Controller 主要负责：

- 接收请求
- 参数处理
- 调用业务逻辑
- 返回结果

不要把大量核心业务逻辑写在 Controller 中。

核心业务逻辑应放在 Service 等合适的位置。

数据库访问逻辑和业务逻辑应合理分离。

---

## 11. API 规则

后端接口采用 REST 风格。

根据语义合理使用 HTTP Method：

- GET：查询
- POST：创建资源或执行操作
- PUT / PATCH：修改
- DELETE：删除

不要所有接口都使用 POST。

接口 URL 命名保持统一。

所有接口必须采用统一的 JSON 返回结构。

例如统一包含：

- code
- message
- data

具体字段可以根据项目设计确定，但整个项目必须统一。

---

## 12. 异常处理规则

后端使用统一异常处理机制。

至少合理处理：

- 参数错误
- 未登录
- 用户不存在
- 打卡项不存在
- 无权访问其他用户数据
- 重复打卡
- 数据库异常
- 服务器内部错误

不要在每个 Controller 中重复编写大量 try/catch。

---

## 13. 配置与安全规则

不得在源码中硬编码：

- MySQL 密码
- Redis 密码
- Token
- Secret
- 其他敏感信息

敏感配置应通过环境变量或其他安全配置方式提供。

不得将真实密码或密钥提交到 Git。

应正确配置 `.gitignore`。

---

## 14. 依赖规则

后端依赖由 Maven 管理。

前端依赖使用项目对应的包管理方式管理。

新增依赖前先判断是否确实需要。

不要为了简单功能引入过重的框架。

优先使用简单、容易理解、容易解释的实现。

---

## 15. 开发原则

项目优先保证：

1. 功能正确
2. 前后端真实联通
3. MySQL 数据真实持久化
4. Redis 被真实使用
5. 连续打卡算法正确
6. 并发打卡安全
7. API 风格统一
8. 错误处理统一
9. 代码清晰
10. 项目可以正常运行

不要为了展示“高级架构”而主动引入没有必要的复杂设计。

除非明确要求，否则不要主动增加：

- 微服务
- 消息队列
- 分布式事务
- 事件驱动架构
- 复杂领域模型
- 过度抽象
- 与本项目无关的 AI 功能

优先实现能够正确满足题目要求的最简单方案。

---

## 16. Codex 修改代码规则

修改代码前，先检查当前仓库结构和已有实现。

不要假设某个：

- 文件
- 接口
- 数据库表
- Redis Key
- 功能

已经存在。

修改项目时：

- 尽量复用现有结构
- 不随意删除已有功能
- 不修改与当前任务无关的代码
- 不进行无意义的大规模重构
- 保持前后端接口一致
- 数据库结构变化时同步修改 SQL
- API 变化时同步修改前端调用
- Redis 设计变化时同步修改相关代码

如果发现明显的设计冲突，应先说明，不要自行改变核心业务规则。

---

## 17. Codex 执行边界

根据当前对话中的任务逐步开发项目。

除非用户明确要求一次性完成多个模块，否则：

- 每次只完成当前明确要求的任务
- 不得因为文档描述了完整系统，就自行继续实现后续模块
- 不得未经用户要求一次性生成整个项目
- 可以提出下一步建议，但不要自行执行下一阶段
- 完成当前任务后停止继续扩展

如果用户要求“规划”，只进行规划，不直接修改代码。

如果用户要求“实现”，则实际修改代码，不要只给出示例。

---

## 18. 完成任务后的检查

完成开发任务后，应根据本次修改检查相关内容。

包括但不限于：

- 前端是否真实调用后端
- 后端是否真实访问 MySQL
- Redis 是否真实参与对应业务
- 数据是否成功持久化
- 重复打卡是否会产生重复数据
- 并发打卡是否安全
- 连续天数计算是否正确
- API 返回格式是否统一
- 错误处理是否统一
- 是否存在硬编码敏感信息
- 项目是否能够正常构建

修改后应尽可能运行相关测试或构建。

如果没有实际执行测试或构建，不要声称已经测试通过。

任务完成后简要说明：

- 修改了哪些内容
- 修改了哪些文件
- 做了哪些验证
- 哪些内容尚未验证